# Copy to .env.local for local dev, or set in your host (e.g. Vercel) for deploy.

# Base URL of the Django backend API.
# Defaults to http://127.0.0.1:8000 if unset (see src/lib/auth.ts).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
